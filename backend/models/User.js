const mongoose = require('mongoose'); const userSchema = new mongoose.Schema({ name: { type: String }, email: { type: String, unique: true, required: true, index: true }, passwordHash: { type: String, required: true }, role: { type: String, enum: ['performer', 'admin'], default: 'performer' } }, { timestamps: true }); module.exports = mongoose.model('User', userSchema);